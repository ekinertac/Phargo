/target
**/*.rs.bk
Cargo.lock.bak

# Vendored upstream PHP test corpus — reproduce with: scripts/fetch-corpus.sh
/vendor

# Harness runtime artifacts
.claude/scheduled_tasks.lock

# Private working notes (launch strategy, etc.) — not for the public repo
notes.md
